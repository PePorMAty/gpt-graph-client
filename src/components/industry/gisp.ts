/**
 * Публичный реестр ГИСП.
 *
 * Прямых ссылок на отдельные записи в выгрузке нет — колонка со ссылкой у неё
 * пустая, — поэтому ведём на страницу реестра, а номер записи показываем
 * рядом. Если понадобится открывать запись сразу, сюда добавится шаблон
 * адреса, и правка будет в одном месте.
 */
export const GISP_REGISTRY_URL = "https://gisp.gov.ru/pp719v2/pub/prod/";

/**
 * Страница кода ОКПД2 в классификаторе.
 *
 * Код сам по себе мало что говорит, а расшифровку мы показываем по своему
 * файлу — и он шестизначный, то есть даёт название группы, а не позиции. На
 * странице классификатора код виден целиком, со всеми уровнями. Там же
 * помечены снятые коды («исключён изменением №133/2026») — этого мы сказать
 * не можем: наш файл плоский, без статусов.
 */
export const okpd2Url = (code: string) =>
  `https://classifikators.ru/okpd/${encodeURIComponent(String(code ?? "").trim())}`;
